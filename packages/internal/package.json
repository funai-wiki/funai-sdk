{
  "private": true,
  "name": "@funai/internal",
  "scripts": {
    "start": "tsc -b tsconfig.build.json --watch --verbose",
    "typecheck": "tsc --noEmit",
    "typecheck:watch": "npm run typecheck -- --watch"
  },
  "devDependencies": {
    "@stacks/blockchain-api-client": "^7.12.0",
    "@funai/network": "^7.0.2",
    "@funai/stacking": "^7.0.3",
    "jest-fetch-mock": "^3.0.3"
  },
  "version": "7.0.3"
}
